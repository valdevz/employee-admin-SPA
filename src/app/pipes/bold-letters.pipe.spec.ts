import { BoldLettersPipe } from './bold-letters.pipe';

describe('BoldLettersPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldLettersPipe();
    expect(pipe).toBeTruthy();
  });
});
